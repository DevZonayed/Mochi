node_modules/
server/node_modules/
.DS_Store
server/.DS_Store
.super-tester/
server/.super-tester/
server/.continuum/
exports/
/mcp/

# Local env / secrets — never commit
.env
.env.*
!.env.example

# Logs
*.log

# Editor / IDE
.vscode/
.idea/
*.swp

# Test coverage
coverage/

.continuum/secrets/
# Transient browser-MCP caches written into the repo's own .continuum by test
# runs (the chain itself — chain/, STATE.md — stays tracked).
.continuum/selectors/
.continuum/runs/
.continuum/verification/
.continuum/.env-provenance.json
